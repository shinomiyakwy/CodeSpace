
Mddle-Agent
  1.curl -o- https://githubusercontent.com | bash
source ~/.bashrc
  2.Bounce public IP bingded
  3.

WebSocket ：
  1.set docker
  2.install node.js into npm package manager
  3.webcom indiviudal certificate
    setting port from local host to commit



#low agentcy
   package localhost
